interface:
  display_name: "How"
  short_description: "Repository architecture, ownership, and runtime flow"
  default_prompt: "Use $how for this request."
policy:
  products:
    - CODEX
  allow_implicit_invocation: false
